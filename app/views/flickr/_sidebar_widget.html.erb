<div class="flickr">
  <% photos.each do |photo| %>
    <% info = flickr.photos.getInfo(photo_id: photo.id) %>
    <%= link_to(image_tag(FlickRaw.url_s(info), class: "flickr_photo", title: photo.title, border: 0, size: "75x75"), FlickRaw.url_photopage(info)) %>
  <% end %>
</div>
